// 함수를 사용해 두 수 더하기
'use strict'

addNumber() // 함수 선언 위치와 상관없이 함수 실행 가능

function addNumber() {
    let num1 = 2
    let num2 = 3
    let sum = num1 + num2
    console.log('결괏값:', sum)
}

addNumber()
